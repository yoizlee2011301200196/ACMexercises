#include <iostream>
#include <vector>
#include <set>
#include <iterator>
#include <string>
#include <algorithm>
using namespace std;

int main()
{
    int num;
    while (cin >> num)
    {
	vector<string> vect;
	string s;
	while (num--)
	{
	    cin >> s;
	    vect.push_back(s);
	}
	string query;
	cin >> query;
	int index;
	cin >> index;
	set<string> allbro;
	
    }

    return 0;
}
